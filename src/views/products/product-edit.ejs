<!DOCTYPE html>
<html lang="es">

<%- include('../partials/head', { title: 'Editar Servicio/Proyecto | FreeLab' }) %>

<body class="body-pe">
    <%- include('../partials/header') %>

    <div class="container-pe mt-5">
        <div class="card edit-product-card">
            <div class="card-header bg-primary text-white">
                <h1 class="mb-0">Editar Servicio/Proyecto</h1>
            </div>
            <div class="card-body-pe">
                <form action="/productos/editar" method="post" enctype="multipart/form-data">

                    <!-- Agregar un input oculto para el ID del producto -->
                    <input type="hidden" name="id" value="<%= product ? product.id : '' %>">

                    <div class="row">
                        <div class="col-12 mb-3">
                            <label for="nombre"  class="form-editpe">Nombre del Servicio/Proyecto</label>
                            <div class="form-floating">
                                <input name="nombre" type="text" class="form-pe" id="nombre"
                                    value="<%= product && product.name || '' %>" required>
                                    
                            </div>
                        </div>

                        <div class="col-12 mb-3">
                            <label for="descripcion"  class="form-editpe">Descripción</label>
                            <div class="form-floating">
                                <textarea name="descripcion" class="form-pe" id="descripcion"
                                    style="height: 150px;"><%= product && product.description || '' %></textarea>
                            </div>
                        </div>

                        <div class="col-12 mb-3">
                            <label for="habilidades"  class="form-editpe">Descripción de habilidades</label>
                            <div class="form-floating">
                                <textarea name="habilidades" class="form-pe" id="habilidades"
                                    style="height: 150px;"><%= product && product.skills || '' %></textarea>
                                
                            </div>
                        </div>
                        
                        <div class="col-12 mb-3">
                            <label for="portfolio"  class="form-editpe">Link a Portfolio</label>
                            <div class="form-floating">
                                <input name="portfolio" type="url" class="form-pe" id="portfolio"
                                    value="<%= product && product.portfolio || '' %>">
                                
                            </div>
                        </div>

                        <div class="col-12 mb-3">
                            <label for="redes_sociales"  class="form-editpe">Links a Redes Sociales</label>
                            <div class="form-floating">
                                <input name="redes_sociales" type="url" class="form-pe" id="redes_sociales"
                                    value="<%= product && product.redes_sociales || '' %>">
                                
                            </div>
                        </div>
                        
                        <div class="col-12 mb-3">
                            <label for="categoria"  class="form-editpe">Categoría</label>
                            <div class="form-floating">
                                <input name="categoria" type="text" class="form-pe" id="categoria"
                                    value="<%= product && product.category || '' %>">
                            </div>
                        </div>

                        <div class="col-12 mb-3">
                            <div class="input-group">
                                <label for="precio" class="form-editpe me-2" >Precio:</label>
                                <div class="input-group">
                                    <span class="input-group-text"> $</span>
                                    <input name="" type="number" class="form-editpe" id="precio"
                                        value="<%= product && product.price || '' %>" required>
                                        <select class="custom-selectpe" id="divisa" name="divisa" required>
                                            <option value="ARS" <%= product && product.divisa === 'ARS' ? 'selected' : '' %>>ARS
                                            </option>
                                            <option value="USD" <%= product && product.divisa === 'USD' ? 'selected' : '' %>>USD
                                            </option>
                                            <option value="EUR" <%= product && product.divisa === 'EUR' ? 'selected' : '' %>>EUR
                                            </option>
                                        </select>
                                </div>
                               
                            </div>
                        </div>

                        <div class="col-12 d-flex justify-content-end">
                            <a href="#" class="btn btn-lg btn-dark-custom">Guardar Cambios</a>
                        </div>


                    </div>
                </form>
            </div>
        </div>
    </div>
    <%- include('../partials/footer') %>

</body>
</html>



